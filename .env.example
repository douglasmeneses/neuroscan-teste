# Copie este arquivo para .env.local e ajuste os valores

# Dev local → aponta direto para a API (porta exposta)
EXPO_PUBLIC_API_URL=http://localhost:3000

# VPS (docker compose) → deixe em branco ou /api (o Nginx faz o proxy internamente)
# EXPO_PUBLIC_API_URL=/api
